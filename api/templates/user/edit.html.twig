{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
<main class="my-5">
    <section class="user-edit py-5">
        <h1 class="text-center mb-3 font-weight-normal">Editer mon profil</h1>

        {{ include('profile/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div class="container d-flex">
            <a class="btn-home" href="{{ path('app_user_index') }}">Retour</a>
            <a class="btn-home" href="{{ path('app_user_delete', {'id': user.id}) }}">Supprimer le compte</a>
        </div>

        
    </section>
</main>

{% endblock %}
