{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
<main class="my-5">
    <section class="user-new py-5">
        <h1 class="text-center mb-3 font-weight-normal">Create new User</h1>

        {# {{ include('user/_form.html.twig') }} #}
        <a class="btn-home" href="{{ path('app_register') }}">nvl utilisateur</a>

        <a class="btn-home" href="{{ path('app_user_index') }}">back to list</a>
    </section>
</main>
{% endblock %}
